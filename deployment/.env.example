# Remote Host Configuration for deploy-remote.sh
# Copy this file to .env and fill in your remote host details

# Remote host IP address (local network IP)
hostIp="192.168.1.100"

# SSH username on remote host
username="pi"

# SSH password (optional if using SSH keys)
password="your-password"

# Optional: SSH domain via Cloudflare Tunnel (for external access)
# SSH_DOMAIN="ssh.your-domain.com"
